package com.ss.ugc.android.editor.preview

enum class EditTypeEnum{
    /**
     * 设置编辑模式，
     * DEFAULT（默认全不可编辑）,
     * VIDEO（视频素材编辑模式）,
     * STICKER（贴纸，文字素材编辑模式）,
     * CROP（裁剪）,
     * VIDEO_MASK(蒙板编辑模式),
     */
    DEFAULT,VIDEO,STICKER,CROP,VIDEO_MASK,TAILORING
}
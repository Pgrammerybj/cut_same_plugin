package com.ss.ugc.android.editor.base.utils

object DraftTypeUtils {

    object TrackType {
        const val TYPE_VIDEO = "video"
        const val TYPE_AUDIO = "audio"
        const val TYPE_STICKER = "sticker"
        const val TYPE_EFFECT = "effect"
        const val TYPE_FILTER = "filter"
    }

    object SegmentType {
        const val TYPE_AUDIO = "audio"
        const val TYPE_EFFECT = "effect"
        const val TYPE_IMAGE = "image"
        const val TYPE_STICKER = "sticker"
        const val TYPE_TAILLEADER = "tail_leader"
        const val TYPE_TEXT = "text"
        const val TYPE_VIDEO = "video"
        const val TYPE_FILTER = "filter"
        const val TYPE_ADJUST = "adjust"
        const val TYPE_TEXT_TEMPLATE = "text_template"
    }

    object MetaType {

        const val TYPE_STICKER_ANIMATION = "sticker_animation"

        const val TYPE_MUSIC = "music"
        const val TYPE_RECORD = "record"
        const val TYPE_EXTRACT_MUSIC = "extract_music"
        const val TYPE_SOUND = "sound"
        const val TYPE_TEXT_TO_AUDIO = "text_to_audio"

        const val TYPE_AUDIO_EFFECT = "audio_effect"

        const val TYPE_AUDIO_FADE = "audio_fade"

        const val TYPE_BEATS = "beats"

        const val TYPE_CANVAS_COLOR = "canvas_color"
        const val TYPE_CANVAS_IMAGE = "canvas_image"
        const val TYPE_CANVAS_BLUR = "canvas_blur"

        const val TYPE_FILTER = "filter"
        const val TYPE_BEAUTY = "beauty"
        const val TYPE_RESHAPE = "reshape"
        const val TYPE_VIDEO_EFFECT = "video_effect"

        const val TYPE_BRIGHTNESS = "brightness" // 亮度
        const val TYPE_CONTRAST = "contrast" // 对比度
        const val TYPE_SATURATION = "saturation" // 饱和度
        const val TYPE_SHARPENING = "sharpen" // 锐化
        const val TYPE_HIGHLIGHT = "highlight" // 高光
        const val TYPE_SHADOW = "shadow" // 阴影
        const val TYPE_COLOR_TEMPERATURE = "temperature" // 色温
        const val TYPE_HUE = "tone" // 色调
        const val TYPE_FADE = "fade" // 褪色
        const val TYPE_LIGHT_SENSATION = "light_sensation" // 光感
        const val TYPE_VIGNETTING = "vignetting" // 暗角
        const val TYPE_PARTICLE = "particle" // 颗粒

        const val TYPE_TEXT_EFFECT = "text_effect" // 文字花字
        const val TYPE_TEXT_BUBBLE = "text_shape" // 文字气泡

        const val TYPE_IMAGE = "image"

        const val TYPE_STICKER = "sticker"

        const val TYPE_TAIL = "tail_leader"

        const val TYPE_TEXT = "text"
        const val TYPE_SUBTITLE = "subtitle"
        const val TYPE_LYRIC = "lyrics"

        const val TYPE_TRANSITION = "transition"

        const val TYPE_VIDEO = "video"
        const val TYPE_VIDEO_IMAGE = "photo"
        const val TYPE_SPEED = "speed"

        const val TYPE_VIDEO_ANIM = "video_animation" // 视频动画

        const val TYPE_MIX_MODE = "mix_mode" // 混合模式

        const val TYPE_ADJUST = "adjust" // 全局调节

        const val TYPE_VIDEO_MASK = "mask" // 视频蒙板

        const val TYPE_VIDEO_CHROMA = "chroma" // 色相抠图

        const val TYPE_TEXT_TEMPLATE = "text_template" // 文字模板
    }

    object KeyFrameType {
        const val TYPE_VIDEO = "video"
        const val TYPE_AUDIO = "audio"
        const val TYPE_STICKER = "sticker"
        const val TYPE_TEXT = "text"
        const val TYPE_FILTER = "filter"
        const val TYPE_ADJUST = "adjust"
    }
}

<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    tools:context=".gallery.camera.VideoPreviewActivity">

    <FrameLayout
        android:id="@+id/fl_confirm_top_bar"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/btn_single_play_back"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginStart="14dp"
            android:layout_marginTop="5dp"
            android:src="@drawable/ic_confirm_back"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
    </FrameLayout>

    <com.cutsame.ui.customview.StickyHolderSurfaceView
        android:id="@+id/videoSurface"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginBottom="60dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/fl_confirm_top_bar" />

    <TextView
        android:id="@+id/tv_confirm_replace_material"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_marginBottom="124dp"
        android:background="@drawable/bg_btn_red_fillet"
        android:drawablePadding="4dp"
        android:gravity="center"
        android:paddingLeft="46dp"
        android:paddingRight="46dp"
        android:text="@string/camera_video_confirm"
        android:textColor="@android:color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent" />

    <CheckBox
        android:id="@+id/cb_confirm_save_material"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="80dp"
        android:button="@drawable/check_box_red_dot"
        android:checked="true"
        android:gravity="center_vertical"
        android:paddingLeft="6dp"
        android:text="@string/camera_video_confirm_save"
        android:textColor="@android:color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>